let arr = [1, 2, 3, 4, 5]; // for loop with a counter (the one with the i, j, k) or a for of loop
let obj = { key1: "value1", key2: "value2" }; // for in loop
let arr = [{ key1: "value1" }, { key2: "value2" }]; // for loop with a counter or for of loop...with a nested for in loop

//for loop with counter
for (let i = 0; i < arr.length; i++) {}

// for of loop
for (let ele of arr) {
}

// for in loop
for (let key in obj) {
}
